using UnityEngine;
using System;

public class TriggerAction : MonoBehaviour
{
    public static Action<GameObject> OnTriggerEnter;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
